node_modules/
dist/
build/
# Vendored third-party repos studied for prior art only (they carry their own .git).
.agents/examples
# Scratch workspace for plan execution (ledgers, briefs, review packages).
.superpowers/
.tmp/
.turbo/
.astro/
# Projected into the docs site from packages/gutenberg by `npm run docs:build`. The READMEs
# it is generated from are the committed source; these copies are not.
docs/src/content/docs/reference/
docs/src/content/docs/agents.md
docs/public/catalog.json
*.log
.DS_Store
.env
.env.*
