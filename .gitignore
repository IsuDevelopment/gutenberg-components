node_modules/
dist/
build/
# Vendored third-party repos studied for prior art only (they carry their own .git).
.agents/examples
# Scratch workspace for plan execution (ledgers, briefs, review packages).
.superpowers/
.tmp/
.turbo/
.astro/
.wrangler/
# Generated by `npm run catalog` from the colocated READMEs, which are the committed source.
# Produced on demand — by `prepack` for the tarball and by `docs:build` for the site — so
# there is no committed copy to fall out of date.
packages/gutenberg/AGENTS.md
packages/gutenberg/catalog.json
# Projected into the docs site from packages/gutenberg by `npm run docs:build`.
docs/src/content/docs/reference/
docs/src/content/docs/agents.md
docs/public/catalog.json
*.log
.DS_Store
.env
.env.*
