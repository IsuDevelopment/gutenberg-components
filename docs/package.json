{
	"name": "docs",
	"private": true,
	"type": "module",
	"description": "Documentation site for @isudev/gutenberg, generated from the colocated component READMEs.",
	"scripts": {
		"sync": "node ../packages/gutenberg/scripts/catalog.ts && node scripts/sync-content.mjs",
		"dev": "npm run sync && astro dev",
		"build": "npm run sync && astro build",
		"preview": "astro preview",
		"astro": "astro",
		"deploy": "wrangler deploy",
		"deploy:dry-run": "wrangler deploy --dry-run"
	},
	"dependencies": {
		"@astrojs/starlight": "^0.41.6",
		"astro": "^7.1.6",
		"starlight-llms-txt": "^0.11.0"
	},
	"devDependencies": {
		"cookie": "^2.0.1",
		"wrangler": "^4.118.0"
	}
}
